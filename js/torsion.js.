<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Torsion of a Round Shaft</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="topbar">
    <a class="back" href="index.html">← Back</a>
    <h1>Torsion of a Round Shaft</h1>
  </header>

  <main class="sim">
    <section class="controls">
      <label>Torque T (N·m)
        <input id="T" type="number" step="1" value="100">
      </label>
      <label>Length L (m)
        <input id="L" type="number" step="0.01" value="1.0">
      </label>
      <label>Diameter d (m)
        <input id="d" type="number" step="0.001" value="0.05">
      </label>
      <label>Shear Modulus G (GPa)
        <input id="G" type="number" step="1" value="80">
      </label>

      <button id="compute">Compute</button>
      <div class="results" id="results">θ = — rad, τ_max = — MPa</div>
    </section>

    <section class="output">
      <canvas id="view" width="720" height="420"></canvas>
      <small class="note">Placeholder graphic. You’ll compute θ = TL/(GJ) and τ_max = 16T/(π d³).</small>
    </section>
  </main>

  <script src="js/torsion.js"></script>
</body>
</html>
